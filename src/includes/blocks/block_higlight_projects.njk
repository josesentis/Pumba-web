{% macro block(data) %}
    <section class="block_higlight_projects" scroll-item>
        BLOCK HIGHLIGHT PROJECTS
        <ul class="block_higlight_projects__items">
            {% for item in data.projects %}
                {% set project = utils.get(all[renderData.language].projects, item.project.ID) %}
                <li>
                    <a class="project __project" href="{{ project.link }}">
                        {{ project.title.rendered }}
                    </a>
                </li>
            {% endfor %}
        </ul>
    </section>
{% endmacro %}